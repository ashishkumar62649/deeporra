"""fcode doctor — placeholder until WP1 implements real checks."""

import typer


def doctor_cmd() -> None:
    """Check dependencies and health."""
    typer.echo("Doctor command implementation has not started.")
    raise typer.Exit(code=1)
